import { CurrencyViewYaroslavPipe } from './currency-view-yaroslav.pipe';

describe('CurrencyViewYaroslavPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyViewYaroslavPipe();
    expect(pipe).toBeTruthy();
  });
});
